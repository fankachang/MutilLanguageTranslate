[pytest]
DJANGO_SETTINGS_MODULE = translation_project.settings
django_find_project = false
pythonpath = translation_project
testpaths = tests
python_files = test_*.py
addopts = -ra -m "not slow"
markers =
    slow: 需要載入 ML 模型的慢速測試（使用 pytest -m slow 執行）

